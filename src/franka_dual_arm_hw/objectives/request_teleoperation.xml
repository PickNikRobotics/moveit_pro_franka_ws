<?xml version="1.0" encoding="UTF-8" ?>
<!--
  Dual-arm teleoperation, scoped to the RIGHT arm only (for now).

  This config runs two namespaced controller_managers (/left, /right). The jog
  behaviors below expose controller_manager action-name ports, so they are
  pointed at /right/controller_manager and drive the right arm.

  NOTE / known limitation: the "Move to Pose" / "Move to Joint State" /
  "Interpolate to Joint State" subtrees (teleop modes 3/4/5) are core MoveIt Pro
  objectives whose internal SwitchController targets the default, un-namespaced
  /controller_manager (no port to override it). In this two-namespace config that
  manager does not exist, so those modes are not expected to work as-is — joint /
  Cartesian jogging (modes 1/2) are the supported right-arm teleop paths here.
  The right arm's joint_trajectory_controller is active at startup (see the
  per-arm launch files), so jogging can switch to/from it.
-->
<root BTCPP_format="4" main_tree_to_execute="Request Teleoperation">
  <BehaviorTree
    ID="Request Teleoperation"
    _description="Handles the different variations of teleoperation from the web UI, scoped to the right arm (/right namespace). Should be used as a subtree with port remapping as part of another Objective."
    _favorite="false"
  >
    <Control ID="Sequence">
      <Action ID="Script" code="teleop_mode := 0" />
      <!--
        DoTeleoperateAction only writes its outputs (controllers / planning_groups /
        tip_links / skip_collision_checks / velocity_scale_factor) once the web UI sends
        its first feedback message. With initial_teleop_mode=1 (joint jog) the jog branch
        below ticks immediately and SwitchController / JointJog read {controllers},
        {planning_groups} and {skip_collision_checks} before that feedback arrives, which
        fails with "blackboard variable ... not found". Seed them here with the right-arm
        defaults so the first tick is valid; DoTeleoperateAction overwrites them on feedback.
        (Script cannot build string vectors, so SetBlackboard is used; a single value is
        read back as a one-element vector.)
      -->
      <Action ID="SetBlackboard" output_key="controllers" value="joint_velocity_controller" />
      <Action ID="SetBlackboard" output_key="planning_groups" value="right_manipulator" />
      <Action ID="SetBlackboard" output_key="tip_links" value="right_fr3_link8" />
      <Action ID="SetBlackboard" output_key="skip_collision_checks" value="false" />
      <Action ID="SetBlackboard" output_key="velocity_scale_factor" value="0.3" />
      <Control ID="Parallel" success_count="1" failure_count="1">
        <Action
          ID="DoTeleoperateAction"
          enable_user_interaction="{enable_user_interaction}"
          user_interaction_prompt="{user_interaction_prompt}"
          initial_teleop_mode="{initial_teleop_mode}"
          current_teleop_mode="{teleop_mode}"
          planning_groups="{planning_groups}"
          controllers="{controllers}"
          tip_links="{tip_links}"
          skip_collision_checks="{skip_collision_checks}"
          velocity_scale_factor="{velocity_scale_factor}"
        />
        <Decorator ID="RepeatUnlessFailureEachTick" num_cycles="-1">
          <Control ID="Sequence">
            <!--Closing and opening the gripper-->
            <Decorator ID="ForceSuccess" _skipIf="teleop_mode != 7">
              <SubTree ID="Close Gripper" _collapsed="true" />
            </Decorator>
            <Decorator ID="ForceSuccess" _skipIf="teleop_mode != 6">
              <SubTree ID="Open Gripper" _collapsed="true" />
            </Decorator>
            <!--Joint sliders interpolate to joint state-->
            <Decorator ID="ForceSuccess" _while="teleop_mode == 5">
              <Control ID="Sequence">
                <Control ID="Fallback" name="root">
                  <Control ID="Sequence">
                    <Action
                      ID="RetrieveJointStateParameter"
                      timeout_sec="-1"
                      joint_state="{target_joint_state}"
                    />
                    <SubTree
                      ID="Interpolate to Joint State"
                      _collapsed="false"
                      target_joint_state="{target_joint_state}"
                      controller_names="{joint_trajectory_controller_name}"
                      controller_action_server="{controller_action_server}"
                      planning_group_name="{default_planning_group}"
                      velocity_scale_factor="{velocity_scale_factor}"
                      execution_pipeline="{execution_pipeline}"
                      acceleration_scale_factor="1.0"
                    />
                    <Action
                      ID="PublishEmpty"
                      topic="/moveit_pro_ui/motion_ended"
                      queue_size="1"
                      use_best_effort="false"
                    />
                  </Control>
                  <Control ID="Sequence">
                    <Action
                      ID="PublishEmpty"
                      topic="/moveit_pro_ui/motion_ended"
                      queue_size="1"
                      use_best_effort="false"
                    />
                    <Action ID="AlwaysFailure" />
                  </Control>
                </Control>
              </Control>
            </Decorator>
            <!--Interactive markers move to pose-->
            <Decorator ID="ForceSuccess" _while="teleop_mode == 4">
              <Control ID="Sequence">
                <Control ID="Fallback" name="root">
                  <Control ID="Sequence">
                    <Action
                      ID="RetrievePoseParameter"
                      timeout_sec="-1"
                      pose="{target_pose}"
                    />
                    <Decorator
                      ID="ForEach"
                      index="{index}"
                      out="{planning_group}"
                      vector_in="{planning_groups}"
                    >
                      <Action ID="AlwaysSuccess" />
                    </Decorator>
                    <Decorator
                      ID="ForEach"
                      index="{index}"
                      out="{tip_link}"
                      vector_in="{tip_links}"
                    >
                      <Action ID="AlwaysSuccess" />
                    </Decorator>
                    <SubTree
                      ID="Move to Pose"
                      _collapsed="false"
                      target_pose="{target_pose}"
                      controller_names="{joint_trajectory_controller_name}"
                      controller_action_name="{controller_action_server}"
                      execution_pipeline="{execution_pipeline}"
                      link_padding="{link_padding}"
                      ik_frame="{tip_link}"
                      planning_group_name="{planning_group}"
                      max_mtc_solutions="{max_mtc_solutions}"
                      max_ik_solutions="{max_ik_solutions}"
                      require_user_approval="true"
                      velocity_scale_factor="0.1"
                    />
                    <Action
                      ID="PublishEmpty"
                      topic="/moveit_pro_ui/motion_ended"
                      queue_size="1"
                      use_best_effort="false"
                    />
                  </Control>
                  <Control ID="Sequence">
                    <Action
                      ID="PublishEmpty"
                      topic="/moveit_pro_ui/motion_ended"
                      queue_size="1"
                      use_best_effort="false"
                    />
                    <Action ID="AlwaysFailure" />
                  </Control>
                </Control>
              </Control>
            </Decorator>
            <!--Waypoint buttons move to joint state-->
            <Decorator ID="ForceSuccess" _while="teleop_mode == 3">
              <Control ID="Sequence">
                <Control ID="Fallback" name="root">
                  <Control ID="Sequence">
                    <Action
                      ID="RetrieveJointStateParameter"
                      timeout_sec="-1"
                      joint_state="{target_joint_state}"
                    />
                    <SubTree
                      ID="Move to Joint State"
                      _collapsed="false"
                      target_joint_state="{target_joint_state}"
                      acceleration_scale_factor="1.0"
                      velocity_scale_factor="{velocity_scale_factor}"
                      controller_action_server="{controller_action_server}"
                      controller_names="{joint_trajectory_controller_name}"
                      joint_group_name="{default_planning_group}"
                      link_padding="{link_padding}"
                      execution_pipeline="{execution_pipeline}"
                      seed="0"
                      keep_orientation_tolerance="0.05"
                      keep_orientation="false"
                    />
                    <Action
                      ID="PublishEmpty"
                      topic="/moveit_pro_ui/motion_ended"
                      queue_size="1"
                      use_best_effort="false"
                    />
                  </Control>
                  <Control ID="Sequence">
                    <Action
                      ID="PublishEmpty"
                      topic="/moveit_pro_ui/motion_ended"
                      queue_size="1"
                      use_best_effort="false"
                    />
                    <Action ID="AlwaysFailure" />
                  </Control>
                </Control>
              </Control>
            </Decorator>
            <!--Cartesian jogging (right arm: /right/controller_manager)-->
            <Control ID="Sequence" _while="teleop_mode == 2">
              <Decorator ID="RepeatUnlessFailureEachTick" num_cycles="-1">
                <Control ID="Sequence">
                  <Action
                    ID="SwitchController"
                    activate_controllers="{controllers}"
                    deactivate_controllers="joint_trajectory_controller"
                    automatic_deactivation="true"
                    controller_list_action_name="/right/controller_manager/list_controllers"
                    controller_switch_action_name="/right/controller_manager/switch_controller"
                    strictness="2"
                  />
                  <Decorator ID="ForceSuccess">
                    <Action
                      ID="PoseJog"
                      controller_names="{controllers}"
                      link_padding="{link_padding}"
                      planning_group_names="{planning_groups}"
                      stop_safety_factor="1.500000"
                      include_octomap="false"
                    />
                  </Decorator>
                </Control>
              </Decorator>
            </Control>
            <!--Joint jogging (right arm: /right/controller_manager)-->
            <Control ID="Sequence" _while="teleop_mode == 1">
              <Decorator ID="RepeatUnlessFailureEachTick" num_cycles="-1">
                <Control ID="Sequence">
                  <Action
                    ID="SwitchController"
                    activate_asap="true"
                    automatic_deactivation="true"
                    controller_list_action_name="/right/controller_manager/list_controllers"
                    controller_switch_action_name="/right/controller_manager/switch_controller"
                    strictness="1"
                    timeout="-1.000000"
                    activate_controllers="{controllers}"
                    deactivate_controllers="joint_trajectory_controller"
                  />
                  <Decorator ID="ForceSuccess">
                    <Decorator ID="RepeatUnlessFailureEachTick" num_cycles="-1">
                      <Control
                        ID="Parallel"
                        success_count="1"
                        failure_count="1"
                      >
                        <Decorator
                          ID="RepeatUnlessFailureEachTick"
                          num_cycles="-1"
                        >
                          <Action
                            ID="GetElementOfVector"
                            vector_in="{controllers}"
                            index="0"
                            element="{first_controller}"
                          />
                        </Decorator>
                        <Action
                          ID="JointJog"
                          controller_name="{first_controller}"
                          link_padding="0.00000"
                          stop_safety_factor="1.500000"
                          skip_collision_checks="{skip_collision_checks}"
                        />
                      </Control>
                    </Decorator>
                  </Decorator>
                </Control>
              </Decorator>
            </Control>
          </Control>
        </Decorator>
      </Control>
    </Control>
  </BehaviorTree>
  <TreeNodesModel>
    <SubTree ID="Request Teleoperation">
      <MetadataFields>
        <Metadata runnable="false" />
        <Metadata subcategory="User Input" />
      </MetadataFields>
      <input_port
        name="controller_action_server"
        default="/right/joint_trajectory_controller/follow_joint_trajectory"
        type="std::string"
      />
      <input_port name="enable_user_interaction" default="false" type="bool">
        Enable interactive user prompts during teleoperation mode selection
      </input_port>
      <inout_port name="max_ik_solutions" default="8" type="unsigned int">
        Maximum number of IK solutions PlanToPose will try to produce for a
        given pose
      </inout_port>
      <inout_port name="max_mtc_solutions" default="1" type="unsigned long">
        Maximum number of MTC solutions to find before returning early (0 to
        find all solutions)
      </inout_port>
      <input_port name="initial_teleop_mode" default="1" type="int" />
      <input_port
        name="joint_trajectory_controller_name"
        default="joint_trajectory_controller"
        type="std::string"
      />
      <input_port
        name="user_interaction_prompt"
        default="Press Abort to return FAILURE, Continue for SUCCESS"
        type="std::string"
      />
      <input_port
        name="default_planning_group"
        default="right_manipulator"
        type="std::string"
      />
      <input_port
        name="default_ik_frame"
        default="right_fr3_link8"
        type="std::string"
      />
      <input_port name="link_padding" default="0.01" type="double" />
      <input_port name="execution_pipeline" default="jtc" type="std::string" />
    </SubTree>
  </TreeNodesModel>
</root>
